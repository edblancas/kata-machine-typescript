[Data Structures Q&A](https://frontendmasters.com/courses/algorithms/data-structures-q-a/)

```typescript
const a: number[] = [];
```

Is an array list, not an array.

get is O(1)
push/pop is O(1)
unshift/shift is O(n), cuz underneath the hood is shifting over n elements
